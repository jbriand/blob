#pragma once

// Gameplay tuning lands here in iteration M2 (see ROADMAP.md). Registered
// ahead of the work so parallel iteration branches share one CMake state.

namespace blob::sim {

} // namespace blob::sim
