#pragma once

// Interest management — the zoom curve and visible-set queries — lands here in
// iteration M6 (see ROADMAP.md). Registered ahead of the work so parallel
// iteration branches share one CMake state.

namespace blob::sim {

} // namespace blob::sim
